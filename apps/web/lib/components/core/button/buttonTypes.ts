/**
 * Button types from Figma design system.
 */

// Button variants from Figma: primary, glow, secondary, ghost
export type ButtonVariant = "primary" | "glow" | "secondary" | "ghost";

// Button sizes from Figma: xs (32px), sm (40px), md (48px), lg (56px)
export type ButtonSize = "xs" | "sm" | "md" | "lg";
